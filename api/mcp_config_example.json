{
  "_comment": "Claude Desktop MCP configuration. Add to ~/Library/Application Support/Claude/claude_desktop_config.json (macOS) or %APPDATA%/Claude/claude_desktop_config.json (Windows) or ~/.config/Claude/claude_desktop_config.json (Linux)",
  "mcpServers": {
    "socketspy": {
      "command": "/usr/local/bin/socketspy",
      "args": ["--mcp", "--iface", "vcan0"]
    }
  }
}
